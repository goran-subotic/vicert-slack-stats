<%= form_for(@job_score) do |f| %>
  <% if @job_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_score.errors.count, "error") %> prohibited this job_score from being saved:</h2>

      <ul>
      <% @job_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job %><br>
    <%= f.text_field :job %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
